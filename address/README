geolocation 是html5的一个新的 api，能实现设备定位。

该api需要在环境下运行，尤其是chrome，否则在chrome里没办法运行。firefox浏览器允许其获取位置即可实现。

这个例子引用了google map的一个端口，可以把位置标注在地图上。这个定位很可能和你现在的位置差很远，比如，走几个小时才到。移动端现在都使用 GPS 进行定位，可以精确到20m-30m的精确度。



